const { addWarn, listWarns, removeWarn, clearWarns, countWarns } = require('../utils/warns');
const { getTimer, setTimer, clearTimer } = require('../utils/prisonService');

module.exports = {
  addWarn,
  listWarns,
  removeWarn,
  clearWarns,
  countWarns,
  getTimer,
  setTimer,
  clearTimer,
};
