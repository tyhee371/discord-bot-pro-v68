# @discordjs/opus has no prebuilt for Node 24 (ABI v137).
# Treat it as optional so install doesn't fail — @discordjs/voice
# falls back to opusscript (pure WASM) automatically.
# Use --include=optional to explicitly install optional deps.
